<div class="row align-self-end mt-5">
  <div class="col">
    <h5><%= @task.title %></h5>
  </div>
  <div class="col">
    <p><span><%= link_to '🖋 Edit', edit_task_path(@task) %></span>  <%= link_to '🗑 Toss', task_path(@task), method: :delete, data: { confirm: "Are you sure?" } %></p>
  </div>
</div>
<div class="row align-self-center">
  <div class="col">
    <p class="alert alert-light">❗️ <%= @task.details %></p>
  </div>
</div>
<div class="row align-self-center">
  <div class="col">
    <p class="text-muted"><%= "✅ This task is complete" if @task.completed? %></p>
  </div>
</div>
<div class="row align-self-center mt-5">
  <div class="col">
    <p class="text-muted">⮑ <%= link_to 'Back to all tasks', tasks_path %></p>
  </div>
</div>
